name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  # Release used to build and publish straight off the tag, with no gates of
  # its own, relying on branch CI having run at some point. A tag on an
  # untested commit could ship. These jobs make the release depend on the
  # same checks CI enforces, against the tagged tree itself.
  gate:
    name: fmt + clippy + unit tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - name: cargo fmt --check
        run: cargo fmt --all -- --check
      - name: cargo clippy
        run: cargo clippy --all-targets --release --locked -- -D warnings
      - name: cargo test
        run: cargo test --release --all-targets --locked
      # cargo-deb and cargo-generate-rpm take the version from Cargo.toml while
      # the tarball is named after the tag; a mismatch would ship mislabelled
      # artifacts under one SHA256SUMS.
      - name: Tag matches Cargo.toml version
        run: |
          set -euo pipefail
          CRATE_VERSION="$(cargo metadata --no-deps --format-version 1 | jq -r '.packages[0].version')"
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          if [ "$CRATE_VERSION" != "$TAG_VERSION" ]; then
            echo "tag v$TAG_VERSION does not match Cargo.toml version $CRATE_VERSION" >&2
            exit 1
          fi

  msrv:
    name: MSRV (1.85)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.85.0
      - uses: Swatinem/rust-cache@v2
      - name: cargo build
        run: cargo build --release --locked

  smoke:
    name: docker smoke tests
    runs-on: ubuntu-latest
    needs: [gate]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Build release binary
        run: cargo build --release --locked
      - name: Build test image
        run: docker build -f tests/Dockerfile -t janitor-test .
      - name: Run smoke tests
        run: docker run --rm janitor-test

  build:
    name: build ${{ matrix.artifact_name }}
    needs: [gate, msrv, smoke]
    # ubuntu-22.04 provides glibc 2.35, so resulting .deb / .tar.gz binaries
    # run on Debian 12 (glibc 2.36), Ubuntu 22.04+, RHEL 9+, and newer.
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            artifact_name: linux-amd64
            use_cross: false
            build_deb: true
            build_rpm: true
          - target: aarch64-unknown-linux-gnu
            artifact_name: linux-arm64
            use_cross: true
            build_deb: true
            build_rpm: true
          - target: x86_64-unknown-linux-musl
            artifact_name: linux-amd64-static
            use_cross: true
            build_deb: false
            build_rpm: false
          - target: aarch64-unknown-linux-musl
            artifact_name: linux-arm64-static
            use_cross: true
            build_deb: false
            build_rpm: false

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          # Include runner OS so cache from newer-glibc runners is not
          # restored onto ubuntu-22.04 (would break `cargo-deb` / `cross`
          # binaries linked against glibc 2.39).
          key: ${{ runner.os }}-${{ matrix.target }}
          cache-bin: false

      - name: Install cross
        if: matrix.use_cross
        run: cargo install cross --locked --version 0.2.5

      # Build a host-native (amd64) janitor first. We need a runnable
      # binary on the runner to invoke `janitor completions` /
      # `janitor man` and generate packaging assets into target/assets/.
      # The cross-compiled arm64 binary we produce in the next step
      # isn't executable here. For amd64 jobs this build is wasted work
      # (the target-specific step rebuilds the same thing), but it's
      # cheap relative to the release tarball upload and keeps the
      # workflow symmetric across targets.
      - name: Build host-native binary (for packaging assets)
        run: cargo build --release --locked

      - name: Generate packaging assets (completions + man page)
        run: |
          set -euo pipefail
          mkdir -p target/assets/completions target/assets/man
          ./target/release/janitor completions bash > target/assets/completions/janitor
          ./target/release/janitor completions zsh  > target/assets/completions/_janitor
          ./target/release/janitor completions fish > target/assets/completions/janitor.fish
          ./target/release/janitor man              > target/assets/man/janitor.1

      - name: Build (cargo)
        if: ${{ !matrix.use_cross }}
        run: cargo build --release --locked --target ${{ matrix.target }}

      - name: Build (cross)
        if: matrix.use_cross
        run: cross build --release --locked --target ${{ matrix.target }}

      - name: Stage binary for packaging tools
        run: |
          mkdir -p target/release
          cp "target/${{ matrix.target }}/release/janitor" target/release/janitor

      - name: Package tarball
        id: tarball
        run: |
          set -euo pipefail
          VERSION="${GITHUB_REF_NAME#v}"
          DIST="janitor-${VERSION}-${{ matrix.artifact_name }}"
          mkdir -p "dist/$DIST"
          cp "target/${{ matrix.target }}/release/janitor" "dist/$DIST/"
          cp README.md LICENSE CHANGELOG.md "dist/$DIST/"
          cp target/assets/man/janitor.1 "dist/$DIST/"
          tar -C dist -czf "dist/$DIST.tar.gz" "$DIST"
          ( cd dist && sha256sum "$DIST.tar.gz" > "$DIST.tar.gz.sha256" )

      - name: Install cargo-deb
        if: matrix.build_deb
        # cargo-deb prebuilt releases are linked against a newer glibc than
        # ubuntu-22.04 provides; compile from source instead. `cache-bin:
        # false` above means this is re-done on each run, which is fine.
        run: cargo install cargo-deb --locked

      - name: Build .deb
        if: matrix.build_deb
        run: |
          set -euo pipefail
          cargo deb --no-build --no-strip --target ${{ matrix.target }} --output dist/
          ( cd dist && for f in *.deb; do sha256sum "$f" > "$f.sha256"; done )

      - name: Install cargo-generate-rpm
        if: matrix.build_rpm
        # A transitive dep of cargo-generate-rpm (num-bigint-dig 0.8.4, via
        # the `rsa` crate used for RPM signing) currently emits a future-
        # incompat warning. It's upstream, not in janitor's own deps, and
        # does not affect the built binary. Tracking:
        #   https://github.com/RustCrypto/RSA/issues (rsa -> num-bigint-dig 0.9)
        run: cargo install cargo-generate-rpm --locked

      - name: Build .rpm
        if: matrix.build_rpm
        run: |
          set -euo pipefail
          cargo generate-rpm --target ${{ matrix.target }} --output dist/
          ( cd dist && for f in *.rpm; do sha256sum "$f" > "$f.sha256"; done )

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: janitor-${{ matrix.artifact_name }}
          path: |
            dist/*.tar.gz
            dist/*.tar.gz.sha256
            dist/*.deb
            dist/*.deb.sha256
            dist/*.rpm
            dist/*.rpm.sha256

  release:
    name: publish release
    needs: [build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Aggregate SHA256SUMS
        run: |
          set -euo pipefail
          cd artifacts
          : > SHA256SUMS
          for f in *.tar.gz *.deb *.rpm; do
            [ -f "$f" ] || continue
            sha256sum "$f" >> SHA256SUMS
          done
          cat SHA256SUMS

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/*.tar.gz
            artifacts/*.deb
            artifacts/*.rpm
            artifacts/SHA256SUMS
          generate_release_notes: true
          body_path: CHANGELOG.md
